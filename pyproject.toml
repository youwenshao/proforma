[project]
name = "proforma-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "numpy",
  "pandas",
  "scipy",
  "pydantic",
  "scikit-learn",
  "joblib",
  "fastapi",
  "uvicorn",
  "httpx",
  "supabase",
]
